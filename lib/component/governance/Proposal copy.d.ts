import { DomNode } from "@hanul/skynode";
import { ProposalInfo } from "../../contracts/VoteContract";
export default class Proposal extends DomNode {
    private proposalId;
    private proposal;
    constructor(proposalId: number, proposal: ProposalInfo);
    private loadResult;
}
//# sourceMappingURL=Proposal%20copy.d.ts.map