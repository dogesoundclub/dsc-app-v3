import { View, ViewParams } from "skyrouter";
export default class Unstaking implements View {
    private container;
    private mateList;
    private mateBalance;
    private mixPrice;
    private selectedMateItem;
    constructor();
    private load;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=Unstaking.d.ts.map