import { View, ViewParams } from "skyrouter";
export default class DscFamily implements View {
    private container;
    constructor();
    private load;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=DscFamily.d.ts.map